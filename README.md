# exceptions

A PyPI module that can easily help you in simply raising BaseException features (i did an error while doing name of module)

## Installation

```bash
pip install expections
```

or

```bash
pip install git
git clone https://github.com/wirnty/expections.git
```

